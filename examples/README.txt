some examples on how to run the hookreceiver.

steps:
- clone the repo
- create a virtualenv
- install the requirements
- install nginx and supervisord
- put hookreceiver.conf to /etc/supervisor/conf.d/ and configure nginx like the example given. 
- register your hooks at bitbucket
